# Current Package Current State Summary

`<current_package>/`은 현재 stable prompt harness package다. active package는 실제 운영 문서, autonomous agent assets, Codex runtime assets, state, verification, lifecycle, validation runner로 구성된다.

## Current status
- current stable: `<current_package>/`
- active package: `<current_package>/`
- evidence package: `_evidence/<current_package>/`
- validation: current runner 기준 pass 상태로 관리

## Current limitations
운영 telemetry, containment proof, provider 다양성 검증은 후속 항목이다.
