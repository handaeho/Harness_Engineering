# Merge Notes

- `AGENTS.md` was absorbed into root `GEMINI.md`.
- `AGENTS.md` is intentionally not included in this Gemini-ready layout to avoid duplicate or conflicting project instruction loading.
- Runtime guide references were normalized to `<project>/GEMINI.md` and `<project>/.gemini/skills/*/SKILL.md`.
- Skill directories were moved under `.gemini/skills/` for Gemini CLI workspace discovery.
