# Runtime

The runtime layer will own orchestration, context building, tool routing,
approval gates, state, sandbox policy, retry, and recovery.

Alpha status:
- Runtime implementation is not included.
- Runtime behavior is not executed.
- The alpha only defines where runtime assets will live.
