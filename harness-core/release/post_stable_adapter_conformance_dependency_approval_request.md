# Adapter Conformance Dependency Approval Request

Status: ready_for_dependency_backed_adapter_conformance

Dependency-backed adapter conformance cannot run unless the local dependency set is available.

Required approval phrase:

```text
I approve local dependency install for adapter conformance validation using npm ci --ignore-scripts.
```

Dependency install executed in this approved flow: true.
