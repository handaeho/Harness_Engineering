# Capabilities

This directory will hold model-independent capability contracts extracted from
v36 overlays.

Alpha status:
- No capability contract is fully migrated yet.
- Capability migration is deferred until the core spec and adapter skeletons are
  statically validated.
