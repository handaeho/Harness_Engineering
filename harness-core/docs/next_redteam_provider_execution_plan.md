# Next Redteam Provider Execution Plan

Provider redteam execution remains pending.

Before provider execution:
- review mock dry-run evidence
- select a narrow provider target
- approve execution scope explicitly
- define request redaction and trace capture
- enforce no tool side effects unless explicitly scoped
- preserve `store:false` where provider calls are used
- keep redteam passed and containment claims blocked until per-case evidence
  and gate checks pass
